#ifndef SECRETS_H
#define SECRETS_H

struct WiFiNetwork {
  const char *ssid;
  const char *pass;
};

WiFiNetwork wifiList[] = {
  {"YOUR_WIFI_SSID_1", "YOUR_WIFI_PASS_1"},
  {"YOUR_WIFI_SSID_2", "YOUR_WIFI_PASS_2"},
};

#define WIFI_COUNT \
  (sizeof(wifiList) / sizeof(wifiList[0]))

#define API_URL \
  "http://YOUR_DOMAIN/audio-device/api/upload.php"

#define CHECK_URL \
  "http://YOUR_DOMAIN/audio-device/api/check.php"

#define DOWNLOAD_URL \
  "http://YOUR_DOMAIN/audio-device/api/download.php"

#define MARK_READ_URL \
  "http://YOUR_DOMAIN/audio-device/api/mark_read.php"

#define API_KEY   "YOUR_API_KEY"
#define DEVICE_ID "YOUR_DEVICE_ID"

#endif
